<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Interface3" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
    </style>

    <style name="AppTheme.ActionBar" parent="Widget.AppCompat.Light.ActionBar">
<!--        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.TitleText</item>
        <item name="titleTextStyle">@style/AppTheme.ActionBar.TitleText</item>

        -->
        <item name="background">@color/colorMenu</item>
        <item name="android:height">150dp</item>
        <item name="height">60dp</item>
    </style>
    <style name="Theme.Interface3" parent="Base.Theme.Interface3" >


    </style>

</resources>